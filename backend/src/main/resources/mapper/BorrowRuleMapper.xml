<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.BorrowRuleMapper">

    <select id="selectByReaderType" parameterType="java.lang.Integer" resultType="com.library.entity.BorrowRule">
        SELECT * FROM borrow_rule WHERE reader_type = #{readerType}
    </select>

</mapper>
